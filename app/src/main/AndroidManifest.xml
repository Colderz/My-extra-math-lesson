<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="pakiet.arkadiuszzimny.extralessonapp1">

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcherjakorepetytor"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_launcherjakorepetytor"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".viewModel.EditableListActivity"></activity>
        <activity android:name=".viewModel.DisplayListActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>